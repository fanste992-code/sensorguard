# SensorGuard Edge Collector Requirements
# ========================================
#
# Install: pip install -r requirements-collector.txt
#
# Note: BAC0 requires Python 3.10+ and has native dependencies.
# On Linux, you may need: apt-get install python3-dev

# ── BACnet Communication ──
BAC0>=2025.9.15                # High-level BACnet/IP library (uses bacpypes3)

# ── HTTP Client ──
httpx>=0.27.0                  # Async HTTP client for cloud API

# ── Configuration ──
pyyaml>=6.0                    # YAML config file support
python-dotenv>=1.0.0           # .env file support (optional)

# ── Optional: Enhanced Output ──
rich>=13.0.0                   # Prettier console output
