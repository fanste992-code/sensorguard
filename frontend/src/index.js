import React from 'react';
import ReactDOM from 'react-dom/client';
import SensorGuardApp from './SensorGuardApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SensorGuardApp />
  </React.StrictMode>
);
